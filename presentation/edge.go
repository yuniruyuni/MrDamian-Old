package presentation

//nolint:revive // this method needs to declare unused argument for export frontend the arg name.
func (a *App) SetAssignment(id string, assginment Assignment) {
	// a.repos.Editor.SetAssignment(id, assginment)
}

//nolint:revive // this method needs to declare unused argument for export frontend the arg name.
func (a *App) AddEdge(source string, target string, sourceHandle string, targetHandle string) {
	// a.repos.Editor.AddEdge(source, target, sourceHandle, targetHandle)
}

//nolint:revive // this method needs to declare unused argument for export frontend the arg name.
func (a *App) RemoveEdge(source string, target string, sourceHandle string, targetHandle string) {
	// a.repos.Editor.RemoveEdge(source, target, sourceHandle, targetHandle)
}
